<%= form_for Picture.new do |f| %>
  <%= f.hidden_field :imageable_id, :value => object.id %>
  <%= f.hidden_field :imageable_type, :value => object.class %>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <%= f.submit %>
<% end %>
